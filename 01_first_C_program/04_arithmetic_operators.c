#include <stdio.h>

int main() {

    // arithmetic operators = + - * / % ++ --

    int x = 10;
    int y = 2;
    int z = 0;

    // Arithmetic Operator

    // z = x + y; // 5
    // z = x - y; // -1
    // z = x * y; // 6
    // z = x / y;
    // z = x % y; // rem--> 0

    // Augmented Assignment Operator

    // x = x + 2;
    // x += 2;
    // x *= 2;
    // x = x * 2;
    // x = x / 2;
    // x /= 2;

    printf("%d\n", x);
}